<style>
    @import url('https://rsms.me/inter/inter.css');

    :root {
        --tblr-font-sans-serif: 'Inter Var', -apple-system, BlinkMacSystemFont, San Francisco, Segoe UI, Roboto, Helvetica Neue,
            sans-serif;

        /* Primary color updated to #007A78 */
        --tblr-primary: #007A78;
        /* Teal for primary color */
        --tblr-primary-hover: #006864;
        /* Slightly darker teal for hover */
        --tblr-primary-active: #005954;
        /* Even darker teal for active */
        --tblr-primary-bg: #d5f0ef;
        /* Light teal for background */
    }

    body {
        font-feature-settings: "cv03", "cv04", "cv11";
    }

    .btn-primary {
        background-color: var(--tblr-primary) !important;
        /* Teal background */
        border-color: var(--tblr-primary) !important;
        /* Teal border */
        color: #fff !important;
        /* White text */
    }

    .btn-primary:hover {
        background-color: var(--tblr-primary-hover) !important;
        /* Darker teal on hover */
        border-color: var(--tblr-primary-hover) !important;
    }

    .btn-primary:active,
    .btn-primary:focus {
        background-color: var(--tblr-primary-active) !important;
        /* Even darker teal for active */
        border-color: var(--tblr-primary-active) !important;
    }

    .text-primary {
        color: var(--tblr-primary) !important;
    }


    .card-hover {
        cursor: pointer;
    }

    .card-hover:hover {
        background-color: rgba(47, 255, 0, 0.1);
        transition: background-color 0.3s ease;
    }


    .file {
        border-radius: 4px;
        cursor: pointer;
    }

    .file:hover,
    .file.selected {
        background-color: rgba(0, 123, 255, 0.1);
        color: var(--tbl-text);
    }

    #custom-lightbox-caption {
        position: fixed;
        bottom: 10%;
        left: 50%;
        transform: translateX(-50%);
        background: rgba(0, 0, 0, 0.7);
        color: white;
        padding: 10px 15px;
        border-radius: 5px;
        font-size: 16px;
        text-align: center;
        max-width: 80%;
        z-index: 99999999999;
    }
</style>